import { styled, Button } from '@mui/material';

export default styled(Button)`
  border-radius: 40;
  background: rgba(0, 43, 180, 0.6);
  font-size: 16px;
  padding: 16px 24px;
  color: white;
  text-transform: capitalize;

  :hover {
    background-color: #76d6cb;
  }
`;
